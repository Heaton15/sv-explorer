[package]
name = "sv-explorer-args"
version = "0.1.0"
edition = "2024"

[build-dependencies] # <-- We added this and everything after!
lalrpop = "0.22.1"

[dependencies]
lalrpop-util = { version = "0.21.0", features = ["lexer", "unicode"] }
logos = "0.15.0"
